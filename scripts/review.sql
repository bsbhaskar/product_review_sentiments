CREATE TABLE reviews (id SERIAL PRIMARY KEY, product_name varchar (300), url varchar (300), page_no integer, r_no integer, r_stars varchar(30), r_date varchar(30), r_name varchar(100), r_title varchar (300), r_text text, r_comments varchar (300) );
